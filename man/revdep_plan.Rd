% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/revdep-plan.R
\name{revdep_plan}
\alias{revdep_plan}
\title{Plan a reverse-dependency check}
\usage{
revdep_plan(
  package,
  recursive = FALSE,
  max_recursive = NULL,
  sample_seed = NULL,
  cache = NULL,
  repos = NULL
)
}
\arguments{
\item{package}{Path to the development package checkout.}

\item{recursive}{Include recursive strong reverse dependencies as candidates.}

\item{max_recursive}{Optional maximum number of recursive-only targets to
select. All direct targets remain selected.}

\item{sample_seed}{Optional non-negative integer used to choose a different
reproducible recursive sample. It can be supplied only with
\code{max_recursive}.}

\item{cache}{Cache directories to inspect for compatible binaries. \code{NULL}
uses the ordinary \code{crancache} directory and the current runner-managed
binary cache when they exist; \code{character()} disables cache inspection.
An explicit value replaces default discovery.}

\item{repos}{Named source repository base URLs. \code{NULL} combines the
configured repositories with the standard Bioconductor repositories. An
explicit value is used exactly. When a named \code{CRAN} repository is present,
reverse targets come from CRAN while dependencies can come from every
configured repository.}
}
\value{
A \code{revdep_plan} list with \code{summary}, \code{targets}, \code{requirements},
\code{unavailable}, and \code{repository_alternates} tables. Per-target requirement
counts overlap; use \code{summary} for totals over unique selected targets and
their installation dependencies in \code{requirements}. Declared system
requirements are metadata clues, not a
platform-readiness check. Repository-unavailable \code{Suggests} remain in
\code{unavailable}, but do not enter preparation requirements.
}
\description{
Discover reverse dependencies and estimate the preparation work without
downloading, building, or checking packages. Direct reverse dependencies
are always selected. Recursive-strong-only targets are opt-in and can be
limited with a reproducible sample.
}
\examples{
\dontrun{
plan <- revdep_plan("/path/to/package")
plan$summary

sampled <- revdep_plan(
  "/path/to/package",
  recursive = TRUE,
  max_recursive = 20
)
sampled$targets
}
}
