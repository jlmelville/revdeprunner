% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/revdep-run.R
\name{revdep_check}
\alias{revdep_check}
\title{Run reverse-dependency comparisons}
\usage{
revdep_check(
  prepared,
  repeat_checks = FALSE,
  worker_timeout_seconds = NULL,
  process_timeout_seconds = 7200L,
  verbose = FALSE
)
}
\arguments{
\item{prepared}{A ready object returned by \code{\link[=revdep_prepare]{revdep_prepare()}}.}

\item{repeat_checks}{Repeat both baseline and candidate checks, including
completed comparisons. Use after repairing external libraries. Compatible
prepared binaries are retained. Defaults to \code{FALSE}, which resumes unfinished
targets and reuses completed changed and unchanged comparisons.}

\item{worker_timeout_seconds}{Positive whole seconds allowed per stock check
worker, or \code{NULL} to derive a budget from preparation evidence.}

\item{process_timeout_seconds}{Positive whole seconds allowed for the entire
stock comparison process, including subject installation. Defaults to 7200.
Also bounds each library restoration or loadability subprocess during admission.
Increasing either budget allows unfinished targets another attempt without
invalidating completed comparisons.}

\item{verbose}{Show phase, package, and completion progress. Defaults to \code{FALSE}.}
}
\value{
A \code{revdep_result} object with \code{summary}, \code{results}, \code{changes}, \code{diagnostics},
the frozen \code{plan}, and advanced \code{evidence}.
\code{summary$elapsed_seconds} measures the stock comparison adapter, excluding
stock initialization.
\code{changes} is a data frame with \code{package}, \code{severity} (\code{error}, \code{warning},
\code{note}), \code{change} (\code{added}, \code{removed}), and \code{message}, using stock's normalized
comparison. These details persist in the saved result. Stock calls a pair
\code{unchanged} when it has no new problems, so it may still have removed problems.
}
\description{
Verify a completed preparation and compare the frozen CRAN baseline with the
current development checkout using the package's guarded stock
\code{revdepcheck} adapter.
}
\details{
Ordinary retries retain complete pairs and retry unfinished pairs. Keep the
run directory to resume an unfinished comparison. Completed results can be
reused without the old comparison workspace or logs. Binary admission checks
the current R major/minor version, platform, architecture, and OS tag;
comparison identity also includes the full R version and candidate source.
After other environmental repairs, use \code{repeat_checks = TRUE} to run both sides
again. Changing candidate hard dependencies or constraints requires a new plan
and preparation; ordinary source edits can reuse preparation.
}
\examples{
\dontrun{
prepared <- revdep_prepare("/path/to/package")
if (nrow(prepared$problems) == 0L) {
  result <- revdep_check(prepared, verbose = TRUE)
  result$results
  result$changes
}
}
}
