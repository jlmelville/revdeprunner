% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/revdep-run.R
\name{revdep_prepare}
\alias{revdep_prepare}
\title{Prepare reverse dependencies}
\usage{
revdep_prepare(
  package,
  recursive = FALSE,
  max_recursive = NULL,
  sample_seed = NULL,
  cache = NULL,
  repos = NULL
)
}
\arguments{
\item{package}{A development package checkout or an existing
\code{\link[=revdep_plan]{revdep_plan()}} object.}

\item{recursive}{Include recursive strong reverse dependencies as candidates.}

\item{max_recursive}{Optional maximum number of recursive-only targets to
select. All direct targets remain selected.}

\item{sample_seed}{Optional non-negative integer used to choose a different
reproducible recursive sample. It can be supplied only with
\code{max_recursive}.}

\item{cache}{Cache directories to inspect for compatible binaries. \code{NULL}
uses the ordinary \code{crancache} directory and prior runner-generated
repositories when they exist; \code{character()} disables cache inspection.}

\item{repos}{Named source repository base URLs. \code{NULL} combines the
configured repositories with the standard Bioconductor repositories. An
explicit value is used exactly. When a named \code{CRAN} repository is present,
reverse targets come from CRAN while dependencies can come from every
configured repository.}
}
\value{
A \code{revdep_prepared} object with \code{summary}, \code{problems}, \code{plan}, and
\code{evidence}. Raw preparation log paths are retained in \code{problems} and the
complete private preparation report is available as advanced evidence.
}
\description{
Freeze and prepare the packages needed for a reverse-dependency comparison.
Preparation stops before any old/new checks begin. If a package cannot be
prepared, the call returns normally with actionable entries in \code{problems}.
Repeat the same call after resolving external prerequisites to resume the
frozen preparation.
}
\details{
The durable data directory defaults to
\code{tools::R_user_dir("revdeprunner", "data")}, and disposable run state
defaults to \code{tools::R_user_dir("revdeprunner", "cache")}. Set
\code{REVDEP_RUNNER_DATA} or \code{REVDEP_RUNNER_RUNS} to override them. Supplying a
plan freezes its selected targets; planning arguments cannot also be
supplied. A checkout call reuses its first matching frozen plan. Create and
pass a new \code{\link[=revdep_plan]{revdep_plan()}} when a refreshed repository snapshot is wanted.
Repository-unavailable \code{Suggests} remain visible in the plan but do not
block preparation, matching stock checks with forced Suggests disabled.

This workflow currently supports Linux. It installs trusted package code in
isolated libraries but is not an operating-system security sandbox.
}
\examples{
\dontrun{
prepared <- revdep_prepare("/path/to/package")
prepared$problems

plan <- revdep_plan(
  "/path/to/package",
  recursive = TRUE,
  max_recursive = 20
)
prepared <- revdep_prepare(plan)
}
}
